{
  "name": "weather-to-telegram-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "zip -r build.zip *",
    "test": "node -e \"console.log(require('./index').handler({}));\"",
    "debug": "node -e \"console.log(require('./index').handler({}));\" --inspect-brk"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "form-data": "^3.0.0"
  }
}
